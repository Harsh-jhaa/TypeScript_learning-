const userinfo = {
    name: 'Jha',
    age: 25,
    isActive: true,
}

function getUserInfo(
    // object as parameter
):{
    // return types as they are object 
} {
    // actual code
    return {}
}